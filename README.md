# Drone

## Conda environment

The `drone_env` Conda environment is recorded in `environment.yml`.
To recreate and activate it:

```bash
conda env create -f environment.yml
conda activate drone_env
```
